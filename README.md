Prerequisites: Node.js, PostgreSQL.

Setup: Mention npm install in both folders.

Database: Provide the CREATE TABLE script clearly.

Environment: Tell them to create a .env file with DATABASE_URL.

Commands: npm test for backend, npm start for both.